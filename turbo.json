{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "semantic-release": {
      "dependsOn": ["^semantic-release"],
      "outputMode": "new-only",
      "outputs": [],
      "inputs": ["*", ":!package.json"]
    }
  }
}
